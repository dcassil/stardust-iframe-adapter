/**
 * Host-local compatibility re-export for the canonical protocol-to-frame-link
 * registry adapter. The public symbol lives in `../protocol/registry.js`.
 */
export type { StardustFrameLinkRegistry } from "../protocol/registry.js";
